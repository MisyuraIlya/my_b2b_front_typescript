import React from 'react';
import './SearchBar.styles.scss';

const SearchBar = () => {
    return (
        <div className='SearchBar'>
            
        </div>
    );
};

export default SearchBar;